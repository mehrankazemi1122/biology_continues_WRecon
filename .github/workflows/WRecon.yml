name: Masscan and ShuffleDNS Installation

on: [push]

jobs:
  install-tools:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install Masscan
      run: |
        git clone https://github.com/blechschmidt/massdns.git
        cd massdns
        make
#        masscan --version

    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.21

    - name: Install ShuffleDNS
      run: |
        go install -v github.com/projectdiscovery/shuffledns/cmd/shuffledns@latest
        shuffledns -version
#    - name: Verify installations
#      run: |
#        masscan --version
#        shuffledns -version

    - name: Install sponge
      run: |
        sudo apt-get install moreutils

    - name: executing DNS Brute
      run: |
        cd ../
        pwd
        cd biology_continues_WRecon 
        ls
        chmod +x script.sh
        ./script.sh temp.txt verisign.com
        ls
        cat prev_output.verisign.com.txt
        
        
    - name: Upload file to GitHub
      uses: actions/upload-artifact@v3
        with:
          name: prev_output.verisign.com.txt
          path: prev_output.verisign.com.txt # Replace with your output file's name
